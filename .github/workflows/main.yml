name: etl job

on:
    push:
        branches:
        - main

jobs:
    job1:
        runs-on: ubuntu-latest
        steps:
        - name: shoe python version
          run: python --version
          
        - name: print message
          run: echo 'job1'
